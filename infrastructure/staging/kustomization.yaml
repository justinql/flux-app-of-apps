apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base/ingress-nginx
  - ../base/minio
patches:
  - path: tenant-release.yaml
    target:
      kind: HelmRelease
  - path: ingress-nginx.yaml
    target:
      kind: HelmRelease
